import 'package:flutter/material.dart';

class H1Heading extends StatelessWidget {
  const H1Heading({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Meu Titulo', style: TextStyle(fontSize: 30));
  }
}
